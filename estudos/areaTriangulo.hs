triangExiste a b c = (a>b+c) && ((a < abs b - c))

heron a b c = sqrt s
    | otherwise = error "não"
    where p = (a+b+c)/2
          s = p*(p-a)*(p-b)*(p-c)


areaTrinag ab b r tipo
    | tipo == "basealtura" && b*h>0 = 
    | tipo == "lados"
    |

